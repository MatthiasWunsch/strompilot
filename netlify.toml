[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (single page; harmless if you add routes later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
